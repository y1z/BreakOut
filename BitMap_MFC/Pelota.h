#pragma once
// Nota averiguar como crear un "class.h" aparte de los demas para no tener que usar struct.
struct Pelota {

public:// Variables 
	int PosicionEnY = 0;
	int PosicionEnX = 0;
	int VelocidadEnY = 0;
	int VelocidadEnX = 0;


};